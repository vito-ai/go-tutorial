# RTZR STT API with SDK

## Requirements
For use of example for Streaming, `Goffmpeg` is needed. First you need to read installation instruction `Goffmpeg` [this link](https://github.com/xfrr/goffmpeg).

And Then,

```bash
go mod download
```